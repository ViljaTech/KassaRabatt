import {StyleSheet} from 'react-native';

export const keyboardAvoidingviewStyles = () => {
  return StyleSheet.create({
    avoidScrollViewStyle: {
      flex: 1,
      flexDirection: 'column',
      justifyContent: 'center',
    },
  });
};
